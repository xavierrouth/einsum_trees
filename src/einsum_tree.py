class TreeNode:
    def __init__(self, name, einsum_string, children=None):
        self.name = name
        self.children = children if children is not None else []
        self.einsum_string = einsum_string
        self.output = [] 
    
    def 
    
